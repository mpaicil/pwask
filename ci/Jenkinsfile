pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        withPythonEnv('python') {
          sh 'python --version'
          sh 'pip install -U pytest'
        }
      }
    }
    stage('test') {
      steps {
        sh 'pytest'
      }   
    }
  }
}